# ESLint configuration for No Isolation projects

## Usage

In `package.json`:

``` JSON
{
  "name": "some-project",
  ...
  "eslintConfig": {
    "extends": "noisolation"
  }
}
```

In `.eslintrc`:

``` JSON
{
  "extends": "noisolation"
}
```
